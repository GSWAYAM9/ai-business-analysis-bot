# Backend - AI Business Analysis Bot

## Setup
1. Create virtual env:
2. Copy `.env.example` to `.env` and fill keys.
3. Run server:
4. API endpoints:
- POST /api/analyze { "url": "https://instagram.com/..." }
- GET /api/history
